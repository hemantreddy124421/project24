class paper
{
 constructor(x,y,r)
 {
     var option={
      isstastic:false,
      restitution:0.3,
      friction:0.5,
      density:1.2  

     }
   this.x=x;
   this.y=y;
   this.r=r
   this.body=bodies.circle(this.x,this.y.this.r/2,options)
   world.add(world,this.body);
} 



}
